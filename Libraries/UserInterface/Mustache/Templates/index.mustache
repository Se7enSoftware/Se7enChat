{{=(( ))=}}
<!DOCTYPE html>
<html ng-app="Se7enChat">
    <head>
        <title>((pageTitle))</title>
        <link rel="stylesheet" type="text/css" href="((cssUrl))/index.css">
        <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.2.8/angular.min.js"></script>
        <script src="http://code.angularjs.org/1.2.8/angular-animate.min.js"></script>
        <script type="text/javascript">
            var user_name = '((user_name))';
            var user_id = ((user_id));
        </script>
        <script src="((javascriptUrl))/index.js"></script>
    </head>
    <body>
        <div id="rooms">
            <span id="roomTitle">Room 1</span>
            <select id="roomSelection">
                ((# rooms ))
                    <option>((name))</option>
                ((/ rooms ))
            </select>
        </div>

        <div ng-controller="PostsController">
            <div id="posts">
                <div class="post" ng-repeat="post in posts" data-ng-animate="'post-animate'">
                    <div class="postHeader">
                        <div id="controlIcons">
                            <img src="((imageUrl))/delete.png" alt="delete" />
                        </div>
                        <strong>{{post.user_name}}</strong> | {{post.timestamp}}
                        <div class="controls"></div>
                    </div>
                    <div class="postBody">
                        {{post.body}}
                    </div>
                </div>
            </div>
            <div id="footerBar">
                <img id="avatar" src="((imageUrl))/default-avatar.png">

                <div id="inputContainer">
                    <form ng-submit="addPost()">
                        <input type="submit" id="messageButton" value="Send">
                        <div id="textContainer">
                            <input
                                type="text"
                                id="messageBar"
                                placeholder="Type here, then press enter."
                                ng-model="postText">
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </body>
</html>
